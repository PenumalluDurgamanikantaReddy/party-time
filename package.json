{
  "name": "my-next-pwa",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build --turbopack",
    "start": "next start",
    "export": "next export",
    "build:export": "next build --turbopack && next export -o out",
    "deploy:firebase": "echo 'Run firebase deploy --only hosting' && exit 1"
  },
  "dependencies": {
    "next": "15.5.5",
    "next-pwa": "^5.6.0",
    "react": "19.1.0",
    "react-dom": "19.1.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "tailwindcss": "^4"
  }
}
